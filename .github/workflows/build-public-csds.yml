name: Build public CSDS data

on:
  push:
    branches: [main]
    paths:
      - ".github/workflows/build-public-csds.yml"
      - "scripts/build_csds_mar25.py"
  workflow_dispatch:

permissions:
  contents: write

concurrency:
  group: build-public-csds
  cancel-in-progress: true

jobs:
  build-csds:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest

    steps:
      - name: 01. Check out repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: 02. Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: 03. Install dependencies
        run: python -m pip install --upgrade pandas requests

      - name: 04. Build validated CSDS totals
        run: python scripts/build_csds_mar25.py

      - name: 05. Commit generated public data
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add public-data/csds-mar-2025.json

          if git diff --cached --quiet; then
            echo "No CSDS-data changes to commit."
          else
            git commit -m "Generate March 2025 CSDS public data"
            git pull --rebase origin main
            git push origin HEAD:main
          fi
