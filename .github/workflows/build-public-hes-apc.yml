name: Build public HES APC data

on:
  push:
    branches: [main]
    paths:
      - ".github/workflows/build-public-hes-apc.yml"
      - "scripts/build_hes_apc_2024_25.py"
  workflow_dispatch:

permissions:
  contents: write

concurrency:
  group: build-public-hes-apc
  cancel-in-progress: true

jobs:
  build-hes-apc:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest

    steps:
      - name: 01. Check out repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: 02. Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: 03. Install dependencies
        run: python -m pip install --upgrade pandas requests openpyxl

      - name: 04. Build final annual HES APC data
        id: build
        continue-on-error: true
        run: |
          set -o pipefail
          python scripts/build_hes_apc_2024_25.py 2>&1 | tee public-data/hes-apc-build-log.txt

      - name: 05. Commit generated public data or diagnostic log
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add public-data/hes-apc-build-log.txt
          if [ -f public-data/hes-apc-2024-25.json ]; then git add public-data/hes-apc-2024-25.json; fi

          if git diff --cached --quiet; then
            echo "No HES APC data changes to commit."
          else
            git commit -m "Record HES APC 2024-25 build result"
            git pull --rebase origin main
            git push origin HEAD:main
          fi

      - name: 06. Fail after preserving diagnostics
        if: steps.build.outcome == 'failure'
        run: exit 1
