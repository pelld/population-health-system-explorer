name: Build public UCR pathway data

on:
  push:
    branches: [main]
    paths:
      - ".github/workflows/build-public-ucr-data.yml"
      - "scripts/build_ucr_2024_25.py"
  workflow_dispatch:

permissions:
  contents: write

concurrency:
  group: build-public-ucr
  cancel-in-progress: true

jobs:
  build-ucr:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest

    steps:
      - name: 01. Check out repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: 02. Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: 03. Install dependencies
        run: python -m pip install --upgrade pandas requests openpyxl

      - name: 04. Build ICB and provider UCR data
        run: python scripts/build_ucr_2024_25.py

      - name: 05. Commit generated public data
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add public-data/ucr-2024-25.json

          if git diff --cached --quiet; then
            echo "No UCR-data changes to commit."
          else
            git commit -m "Generate public UCR pathway data"
            git pull --rebase origin main
            git push origin HEAD:main
          fi
